{
  "id": "54922e1d",
  "title": "Design a pager-compatible mode for git diff style usage",
  "tags": [
    "feature",
    "pager",
    "compatibility"
  ],
  "status": "closed",
  "created_at": "2026-03-18T13:09:07.937Z"
}

Completed on `master`.

Delivered:
- Added a shared `--pager` CLI option.
- Pager mode hides app chrome like the menu bar and status bar.
- Pager mode disables mouse input at renderer startup.
- Pager mode keeps the diff view responsive to viewport width while hiding the files pane.
- Pager mode limits controls to pager-style navigation and quit behavior.
- Added pager-style paging keys to regular mode too (`space`/`b`, Home/End, PageUp/PageDown support in the app handler).
- Added tests covering CLI parsing and pager-mode responsive chrome/layout behavior.
