{
  "id": "7bdd8933",
  "title": "Clear the terminal cleanly when quitting hunk",
  "tags": [
    "ux",
    "terminal",
    "quit"
  ],
  "status": "closed",
  "created_at": "2026-03-18T13:09:07.937Z"
}

Completed on `master`.

Delivered:
- Routed app quit paths through an injected shutdown callback instead of raw `process.exit` calls.
- Added a shared `shutdownSession()` helper that unmounts the React root, destroys the renderer, clears the restored terminal, and exits.
- Updated `src/main.tsx` to use the cleanup-aware shutdown flow.
- Added tests covering tty and non-tty shutdown behavior.
